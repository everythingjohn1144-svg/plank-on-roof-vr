using UnityEngine;

public class Top_Floor_button6 : MonoBehaviour
{
   /* public lift_movement lift;      // drag your lift GameObject here
    public int floorNumber;         // set 0–5 per button in Inspector
    public string handTag = "Hand"; // or "Player" — whatever you tag your controller/hand collider

    private bool isCoolingDown = false;
    public float cooldown = 1.0f;

    private void OnTriggerEnter(Collider other)
    {
        if (isCoolingDown) return;
        if (!other.CompareTag(handTag)) return;

        lift.GoToFloor(floorNumber);
        isCoolingDown = true;
        Invoke(nameof(ResetCooldown), cooldown);
    }

    private void ResetCooldown()
    {
        isCoolingDown = false;
    } */
}
